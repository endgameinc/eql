[wheel]
universal = 1

[flake8]
max-line-length = 120
max-complexity = 23
# Exempt style/docstring issues to avoid bloating PR
# F405: star imports - code works, just can't detect undefined names
# D401: docstring imperative mood - style preference
# D300: triple double quotes - style preference
# F403: star imports used - intentional for convenience
# E721: type comparisons - many are intentional (exact type checks in __eq__, etc.)
ignore = F405, D401, D300, F403, E721

[pep257]
ignore = D203

[tool:pytest]
addopts = --cov=eql --cov-report term-missing --cov-report=xml --cov-report=html --junitxml=junit.xml -v
